# Intelligent-GST-Reconciliation-Using-Knowledge-Graphs
To connect HTML, CSS, and JavaScript with FastAPI, the frontend communicates with the backend using HTTP requests. When a user performs an action on the webpage, JavaScript sends data to a FastAPI endpoint using fetch(). FastAPI processes the 
